---
title: "chad_huntebrinker_hw1"
author: "Chad Huntebrinker"
date: "2024-09-06"
output:
  word_document: default
  html_document: default
---


Question 1: Create a vector x. x contains a sequence of integers from 11 to 16.
```{r}
x <- c(11:16)
x
```


Question 2: Name the elements of x using words: eleven, twelve, thirteen, fourteen, fifteen and sixteen.
```{r}
names(x) <- c("eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen")
x
```


Question 3: Index the element "twelve - 12" by its name.
```{r}
x["twelve"]
```

Question 4: Index the 3rd and 4th elements in the vector.
```{r}
x[c(3,4)]
```

Question 5: Remove the names in the x vector.
```{r}
names(x) <- NULL
x
```

Question 6: Use filtering to subset values that has a square value larger than or equal to 144.
```{r}
subset_of_x <- x[x^2 >= 144]
subset_of_x
```

Question 7: Coerce the vector x into a matrix x of 3 rows and 2 columns.
```{r}
matrix_x <- matrix(x, nrow = 3, ncol = 2)
matrix_x
```

Question 8: Transpose the matrix and save it into object y
```{r}
y <- t(matrix_x)
y
```

Question 9: Calculate the mean of each row for x using the apply() function.
```{r}
mean_of_rows <- apply(matrix_x, 1, mean)
mean_of_rows
```

Question 10: Apply function of your own (user defined function)  as argument for apply() and explain it.
```{r}
#The purpose of this function is to find the hypotenuse of a triangle.
#It assumes it is getting a matrix with two columns (each column being a triangle's side). 
#Then we solve for c in the Pythagorean Theorem (a^2 + b^2 = c^2) which gives us:
#c = sqrt(a^2 + b^2)
find_hypotenuse <- function(x) {
  return(sqrt(x[1]^2 + x[2]^2))
}

#create a matrix that has the values for the sides of a triangle
triangle_matrix <- matrix(c(6, 21, 8, 72), nrow = 2, ncol = 2)
print("triangle_matrix")
triangle_matrix

#this apply function will use the find_hypotenuse function above and store the answer
hypotenuse_answer <- apply(triangle_matrix, 1, find_hypotenuse)
print("hypotenuse_answer")
hypotenuse_answer

```

